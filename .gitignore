# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
build/
dist/

# production
*.log
dev.log
server.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# database (local only — use Turso in production)
db/*.db
db/*.db-journal
prisma/db/
*.db
*.db-journal

# uploaded receipts (legacy local storage; now stored in DB)
public/uploads/

# env files (set real values in Vercel dashboard or local .env)
.env
.env.local
.env*.local

# vercel
.vercel

# os / editor
.DS_Store
*.pem
.idea/
.vscode/
*.swp

# sandbox scratch
download/
agent-ctx/

# skill definitions (dev-environment only, not part of the shop)
skills/
examples/

# lockfiles — project uses bun
package-lock.json
yarn.lock
pnpm-lock.yaml
